// Esse script é mantido para compatibilidade com o código antigo.
// As funcionalidades principais foram migradas para o componente React KYCValidator.
document.addEventListener('DOMContentLoaded', function() {
    console.info('KYC Main script loaded');
});